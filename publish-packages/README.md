# publish-packages action

Runs the `publish-packages` monochange GitHub Action variant.

## Inputs

- `stream-output` - pass `--stream-output` to `monochange step publish-packages` so package-manager stdout and stderr stream into the workflow log while still being captured in the publish report.
